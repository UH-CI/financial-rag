server {
    listen 80 default_server;
    server_name 128.171.215.58;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl default_server;
    server_name 128.171.215.58;

    ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt;
    ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key;
    ssl_dhparam /etc/ssl/certs/dhparam.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    root /home/exouser/RAG-system/frontend/dist;
    index index.html;

    # 🔁 API reverse proxy - strip /api/ prefix
    location ~ ^/api/?(.*)$ {
        proxy_pass http://127.0.0.1:8200/$1$is_args$args;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 🔌 WebSocket support
    location /ws {
        proxy_pass http://127.0.0.1:8200/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket specific timeouts
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }

    location / {
        try_files $uri /index.html;
    }
}

server {
    listen 443 ssl;
    server_name 128.171.215.58.sslip.io; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/128.171.215.58.sslip.io/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/128.171.215.58.sslip.io/privkey.pem; # managed by Certbot
    ssl_dhparam /etc/ssl/certs/dhparam.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    root /home/exouser/RAG-system/frontend/dist;
    index index.html;

    # 🔁 API reverse proxy - strip /api/ prefix
    location ~ ^/api/?(.*)$ {
        proxy_pass http://127.0.0.1:8200/$1$is_args$args;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 🔌 WebSocket support
    location /ws {
        proxy_pass http://127.0.0.1:8200/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket specific timeouts
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }

    location / {
        try_files $uri /index.html;
    }
}

server {
    listen 443 ssl;
    server_name finbot.its.hawaii.edu; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/finbot.its.hawaii.edu/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/finbot.its.hawaii.edu/privkey.pem; # managed by Certbot
    ssl_dhparam /etc/ssl/certs/dhparam.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    root /home/exouser/RAG-system/frontend/dist;
    index index.html;

    # 🔁 API reverse proxy - strip /api/ prefix
    location ~ ^/api/?(.*)$ {
        proxy_pass http://127.0.0.1:8200/$1$is_args$args;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 🔌 WebSocket support
    location /ws {
        proxy_pass http://127.0.0.1:8200/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket specific timeouts
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }

    location / {
        try_files $uri /index.html;
    }
}

